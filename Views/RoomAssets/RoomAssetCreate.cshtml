@model do_an_tot_nghiep.Models.RoomAsset
@{
    ViewData["Title"] = "Thêm thiết bị phòng";
}

<div class="container-fluid">
    <div class="page-header">
        <h3 class="page-title text-gradient">Thêm thiết bị phòng</h3>
        <p class="text-muted">Nhập thông tin thiết bị để quản lý theo phòng</p>
    </div>

    <div class="card main-card p-4">
        <form asp-action="Create" asp-controller="RoomAssets" method="post" id="assetForm">
            @Html.AntiForgeryToken()

            <partial name="~/Views/Shared/_RoomAssetForm.cshtml" model="Model" />

            <div class="d-flex gap-2">
                <button type="submit" class="btn btn-grad" id="btnSubmit">
                    <span class="btn-text">Lưu</span>
                    <span class="spinner-border spinner-border-sm d-none"></span>
                </button>

                <a asp-action="Index"
                   asp-route-roomId="@Model.RoomId"
                   class="btn btn-outline-brand">Quay lại</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        $('#assetForm').on('submit', function () {
            $('#btnSubmit').prop('disabled', true);
            $('#btnSubmit .btn-text').text('Đang xử lý...');
            $('#btnSubmit .spinner-border').removeClass('d-none');
        });
    </script>
}
