@{
    Layout = null;
}
<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SmartStay - Trang Chủ Khách Thuê</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" rel="stylesheet">
    <style>
        :root {
            --brand-500: #00b894;
            --brand-600: #00a383;
            --accent-500: #0984e3;
            --sun-500: #fdcb6e;
            --ink-900: #2d3436;
            --ink-600: #636e72;
            --bg: #f5f6fa;
            --card: #ffffff;
            --border: #e6e8ef;
            --gradient: linear-gradient(135deg, #00b894 0%, #0984e3 100%);
            --shadow: 0 15px 35px rgba(0, 0, 0, 0.08);
        }

        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            font-family: 'Inter', sans-serif;
            color: var(--ink-900);
            background: var(--bg);
        }

        a { text-decoration: none; color: inherit; }

        .navbar {
            position: sticky;
            top: 0;
            z-index: 100;
            background: rgba(255,255,255,0.95);
            backdrop-filter: blur(10px);
            border-bottom: 1px solid var(--border);
        }

        .nav-inner {
            max-width: 1200px;
            margin: 0 auto;
            padding: 16px 20px;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        .brand {
            display: flex;
            align-items: center;
            gap: 10px;
            font-weight: 800;
            font-size: 1.2rem;
        }

        .brand-badge {
            width: 38px;
            height: 38px;
            border-radius: 12px;
            background: var(--gradient);
            color: #fff;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 8px 20px rgba(0, 184, 148, 0.3);
        }

        .nav-links {
            display: flex;
            gap: 18px;
            font-weight: 600;
            color: var(--ink-600);
        }

        .nav-links a:hover { color: var(--brand-600); }

        .hero {
            max-width: 1200px;
            margin: 0 auto;
            padding: 70px 20px 40px;
            display: grid;
            grid-template-columns: 1.1fr 0.9fr;
            gap: 40px;
            align-items: center;
        }

        .hero h1 {
            font-size: 2.6rem;
            line-height: 1.2;
            margin-bottom: 16px;
        }

        .hero p {
            color: var(--ink-600);
            font-size: 1.05rem;
        }

        .hero-card {
            background: var(--card);
            border-radius: 20px;
            box-shadow: var(--shadow);
            padding: 26px;
            border: 1px solid var(--border);
        }

        .search-row {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 12px;
            margin-top: 16px;
        }

        .input {
            border: 1px solid var(--border);
            border-radius: 12px;
            padding: 12px 14px;
            font-size: 0.95rem;
            width: 100%;
        }

        .btn {
            border: none;
            border-radius: 12px;
            padding: 12px 16px;
            font-weight: 600;
            cursor: pointer;
        }

        .btn-primary {
            background: var(--gradient);
            color: #fff;
            box-shadow: 0 10px 20px rgba(0, 184, 148, 0.25);
        }

        .btn-outline {
            background: #fff;
            color: var(--brand-600);
            border: 2px solid var(--brand-600);
        }

        .stats {
            max-width: 1200px;
            margin: 0 auto;
            padding: 10px 20px 40px;
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 16px;
        }

        .stat {
            background: var(--card);
            padding: 18px;
            border-radius: 16px;
            border: 1px solid var(--border);
            box-shadow: var(--shadow);
        }

        .stat h3 { margin: 0; font-size: 1.6rem; }
        .stat p { margin: 6px 0 0; color: var(--ink-600); }

        .features {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px 20px 60px;
        }

        .feature-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 18px;
            margin-top: 18px;
        }

        .feature-card {
            background: var(--card);
            border-radius: 18px;
            padding: 20px;
            border: 1px solid var(--border);
            box-shadow: var(--shadow);
        }

        .feature-card i {
            color: var(--brand-600);
            font-size: 1.4rem;
        }

        .cta {
            max-width: 1200px;
            margin: 0 auto 60px;
            padding: 30px 20px;
        }

        .cta-box {
            background: var(--gradient);
            color: #fff;
            border-radius: 20px;
            padding: 28px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 20px;
            box-shadow: 0 20px 40px rgba(0, 184, 148, 0.25);
        }

        .cta-box h3 { margin: 0 0 6px; }

        @@media (max-width: 980px) {
            .hero { grid-template-columns: 1fr; }
            .stats { grid-template-columns: 1fr; }
            .feature-grid { grid-template-columns: 1fr; }
            .cta-box { flex-direction: column; align-items: flex-start; }
            .search-row { grid-template-columns: 1fr; }
            .nav-links { display: none; }
        }
    </style>
</head>
<body>
    <header class="navbar">
        <div class="nav-inner">
            <div class="brand">
                <span class="brand-badge"><i class="fa-solid fa-house"></i></span>
                SmartStay
            </div>
            <nav class="nav-links">
                <a href="#search">Tìm phòng</a>
                <a href="#features">Tiện ích</a>
                <a href="#support">Hỗ trợ</a>
            </nav>
            <button class="btn btn-outline">Đăng nhập</button>
        </div>
    </header>

    <section class="hero" id="search">
        <div>
            <h1>Tìm phòng trọ phù hợp nhanh chóng và minh bạch</h1>
            <p>SmartStay giúp bạn tra cứu phòng trọ theo vị trí, giá và tiện ích. Thông tin được cập nhật rõ ràng, hỗ trợ đặt lịch xem phòng thuận tiện.</p>
            <div style="margin-top: 18px; display:flex; gap: 12px; flex-wrap: wrap;">
                <button class="btn btn-primary">Khám phá phòng trọ</button>
                <button class="btn btn-outline">Đặt lịch xem phòng</button>
            </div>
        </div>
        <div class="hero-card">
            <h3 style="margin:0 0 10px;">Tìm kiếm nhanh</h3>
            <p style="margin:0; color: var(--ink-600);">Nhập tiêu chí để lọc phòng trọ phù hợp</p>
            <div class="search-row">
                <input class="input" placeholder="Khu vực" />
                <input class="input" placeholder="Ngân sách" />
                <input class="input" placeholder="Số người" />
                <input class="input" placeholder="Tiện ích" />
            </div>
            <button class="btn btn-primary" style="margin-top: 14px; width: 100%;">Tìm phòng ngay</button>
        </div>
    </section>

    <section class="stats">
        <div class="stat">
            <h3>150+</h3>
            <p>Phòng trọ đang trống</p>
        </div>
        <div class="stat">
            <h3>98%</h3>
            <p>Khách thuê hài lòng</p>
        </div>
        <div class="stat">
            <h3>24/7</h3>
            <p>Hỗ trợ giải đáp</p>
        </div>
    </section>

    <section class="features" id="features">
        <h2>Tiện ích nổi bật</h2>
        <div class="feature-grid">
            <div class="feature-card">
                <i class="fa-solid fa-location-dot"></i>
                <h4>Vị trí rõ ràng</h4>
                <p>Thông tin địa chỉ minh bạch, tiện theo dõi bản đồ và di chuyển.</p>
            </div>
            <div class="feature-card">
                <i class="fa-solid fa-wallet"></i>
                <h4>Giá cả hợp lý</h4>
                <p>So sánh giá theo khu vực và ngân sách phù hợp với nhu cầu.</p>
            </div>
            <div class="feature-card">
                <i class="fa-solid fa-shield-heart"></i>
                <h4>An toàn & uy tín</h4>
                <p>Thông tin phòng được kiểm tra, hỗ trợ kết nối nhanh với chủ trọ.</p>
            </div>
        </div>
    </section>

    <section class="cta" id="support">
        <div class="cta-box">
            <div>
                <h3>Cần hỗ trợ tìm phòng?</h3>
                <p>Liên hệ đội ngũ SmartStay để được tư vấn nhanh chóng.</p>
            </div>
            <button class="btn btn-outline">Liên hệ ngay</button>
        </div>
    </section>
</body>
</html>
